# Tuning Guide

> _Filled in during milestone M7. For each vLLM knob (`gpu_memory_utilization`,
> `block_size`, `kv_cache_dtype`, `max_num_seqs`, `max_num_batched_tokens`,
> `enable_chunked_prefill`, `preemption_mode`) this will explain what it does,
> why it affects throughput/tail latency, and how much it moved the needle here._
