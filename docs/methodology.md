# Methodology

> _Filled in during milestone M7. This file will define exactly how we measure
> latency, what the "100ms p99" SLO means (p99 of inter-token latency), how the
> Poisson/bursty load is generated, and the caveats of the comparison._
