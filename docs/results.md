# Results

> _Filled in after the final A100 sweep (milestone M6/M7) with the real measured
> numbers: baseline vs. tuned throughput at the fixed p99 SLO, the speedup ratio,
> and the latency-vs-throughput curves._
