"""Named vLLM engine config presets and the sweep grid.

Populated in milestone M3. The BASELINE preset is deliberately naive so the
tuning gains are honest and attributable; TUNED turns the knobs documented in
docs/tuning-guide.md.
"""
